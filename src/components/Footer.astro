<footer class="site-footer">
  <div class="shell footer-grid">
    <div>
      <p class="footer-mark">vuln.management</p>
      <p class="footer-note">Risk does not fit neatly in a pie chart.</p>
    </div>
    <div class="footer-links" aria-label="Footer navigation">
      <a href="/advisories/">Advisories</a>
      <a href="/scanner/">Secret Scanner</a>
      <a href="/posts/">Posts</a>
      <a href="/memes/">Memes</a>
      <a href="/rss.xml">RSS</a>
    </div>
    <div class="footer-meta">
      <a href="mailto:contact@vuln.management">contact@vuln.management</a>
      <span>Set in Newsreader &amp; IBM Plex Mono</span>
      <span>Printed on 100% recycled pixels</span>
      <span>© {new Date().getFullYear()} vuln.management</span>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    padding-block: 3rem 2.5rem;
    border-top: 3px double var(--line-strong);
    background: var(--paper-deep);
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1.3fr 0.7fr 1fr;
    gap: 2rem;
  }

  .footer-mark {
    margin: 0;
    font-family: var(--serif);
    font-size: 1.6rem;
    font-weight: 800;
    letter-spacing: -0.02em;
  }

  .footer-note {
    margin: 0.35rem 0 0;
    color: var(--muted);
    font-family: var(--serif);
    font-size: 1.25rem;
    font-style: italic;
  }

  .footer-links,
  .footer-meta {
    display: grid;
    align-content: start;
    gap: 0.5rem;
    font-size: 0.82rem;
  }

  .footer-links a {
    width: max-content;
    font-family: var(--mono);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-decoration: none;
    text-transform: uppercase;
  }

  .footer-links a:hover { color: var(--signal-dark); }

  .footer-meta {
    color: var(--muted);
    font-family: var(--mono);
    font-size: 0.66rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .footer-meta a {
    color: var(--ink);
    text-transform: none;
  }

  @media (max-width: 700px) {
    .footer-grid {
      grid-template-columns: 1fr 1fr;
    }

    .footer-meta {
      grid-column: 1 / -1;
    }
  }
</style>
