<footer class="site-footer">
  <div class="section-shell footer-grid">
    <div>
      <p class="footer-mark display">vuln<em>.management</em></p>
      <p class="footer-note">Risk does not fit neatly in a pie chart.</p>
      <p class="footer-status mono"><span class="live-dot"></span> ALL SYSTEMS NOMINAL</p>
    </div>
    <nav class="footer-links" aria-label="Footer navigation">
      <a href="/advisories/">Advisories</a>
      <a href="/scanner/">Secret Scanner</a>
      <a href="/posts/">Posts</a>
      <a href="/memes/">Memes</a>
      <a href="/rss.xml">RSS</a>
    </nav>
    <div class="footer-meta mono">
      <a href="mailto:contact@vuln.management">contact@vuln.management</a>
      <span>Set in Space Grotesk &amp; IBM Plex Mono</span>
      <span>Broadcast from deep field</span>
      <span>© {new Date().getFullYear()} vuln.management</span>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: 6rem;
    padding: 3.5rem 0 2.5rem;
    border-top: 1px solid var(--hairline);
    background:
      radial-gradient(800px 300px at 50% 0%, rgba(255, 106, 26, 0.08), transparent 70%),
      var(--abyss);
    position: relative;
    overflow: hidden;
  }
  .site-footer::before {
    content: "";
    position: absolute;
    top: 0; left: 10%; right: 10%;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--orange), transparent);
    opacity: 0.6;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 2.5rem;
    align-items: start;
  }

  .footer-mark {
    margin: 0;
    font-size: 1.9rem;
    letter-spacing: -0.03em;
  }
  .footer-mark em { font-style: normal; color: var(--orange); }
  .footer-note {
    margin: 0.6rem 0 0;
    color: var(--muted);
    font-size: 0.95rem;
    max-width: 26ch;
  }
  .footer-status {
    margin-top: 1.1rem;
    font-size: 0.68rem;
    letter-spacing: 0.22em;
    color: var(--lime);
    display: inline-flex;
    align-items: center;
    gap: 0.55em;
  }
  .live-dot {
    width: 7px; height: 7px; border-radius: 50%;
    background: var(--lime);
    box-shadow: 0 0 10px var(--lime);
    animation: pulse-dot 1.6s ease-in-out infinite;
  }

  .footer-links {
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
  }
  .footer-links a {
    color: var(--muted);
    text-decoration: none;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    width: fit-content;
    transition: color 0.18s, transform 0.18s;
  }
  .footer-links a:hover { color: var(--orange-hot); transform: translateX(4px); }

  .footer-meta {
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
    font-size: 0.74rem;
    letter-spacing: 0.06em;
    color: var(--faint);
  }
  .footer-meta a { color: var(--muted); text-decoration: none; }
  .footer-meta a:hover { color: var(--cyan); }

  @media (max-width: 760px) {
    .footer-grid { grid-template-columns: 1fr; gap: 1.8rem; }
  }
</style>
