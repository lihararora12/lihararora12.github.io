<footer class="zine-footer">
  <div class="section-shell footer-grid">
    <div>
      <p class="footer-mark display">vuln<em>.management</em></p>
      <p class="footer-note">Risk does not fit neatly in a pie chart.</p>
      <p><span class="sticker teal">Fresh issue every weekday</span></p>
    </div>
    <nav class="footer-links" aria-label="Footer navigation">
      <a href="/advisories/">Advisories</a>
      <a href="/scanner/">Secret Scanner</a>
      <a href="/posts/">Posts</a>
      <a href="/memes/">Memes</a>
      <a href="/rss.xml">RSS</a>
    </nav>
    <div class="footer-meta">
      <a href="mailto:contact@vuln.management">contact@vuln.management</a>
      <span>Set in Archivo Black &amp; IBM Plex Mono</span>
      <span>Printed on 100% recycled pixels</span>
      <span>© {new Date().getFullYear()} vuln.management</span>
    </div>
  </div>
</footer>

<style>
  .zine-footer {
    margin-top: 5rem;
    padding: 3.5rem 0 2.5rem;
    background: var(--ink);
    color: var(--paper);
    border-top: 6px solid var(--tomato);
    position: relative;
    overflow: hidden;
  }
  .zine-footer::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: radial-gradient(rgba(246, 239, 220, 0.07) 1.2px, transparent 1.3px);
    background-size: 22px 22px;
    pointer-events: none;
  }
  .footer-grid {
    position: relative;
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 2.5rem;
    align-items: start;
  }
  .footer-mark {
    margin: 0;
    font-size: 2rem;
    color: var(--paper);
    transform: rotate(-1deg);
  }
  .footer-mark em { font-style: normal; color: var(--tomato); }
  .footer-note {
    margin: 0.7rem 0 1.1rem;
    color: #cbbfa4;
    font-size: 0.95rem;
    font-weight: 600;
    max-width: 26ch;
  }
  .footer-links { display: flex; flex-direction: column; gap: 0.4rem; }
  .footer-links a {
    width: fit-content;
    padding: 0.3rem 0.8rem;
    border: 2px solid transparent;
    border-radius: 999px;
    color: #e8dfc8;
    text-decoration: none;
    font-family: var(--font-display);
    font-size: 0.78rem;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    transition: all 0.15s;
  }
  .footer-links a:hover {
    background: var(--mustard);
    color: var(--ink);
    border-color: var(--paper);
    transform: rotate(-1.5deg) translateX(4px);
  }
  .footer-meta {
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.05em;
    color: #a2937a;
  }
  .footer-meta a { color: #e8dfc8; text-decoration: none; font-weight: 700; }
  .footer-meta a:hover { color: var(--mustard); }

  @media (max-width: 760px) {
    .footer-grid { grid-template-columns: 1fr; gap: 1.8rem; }
  }
</style>
